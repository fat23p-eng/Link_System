วิธีใช้งาน — เปิดไฟล์ด้วย Notepad แก้บรรทัดนี้:
const API_URL = 'https://script.google.com/macros/s/ใส่_URL_ตรงนี้/exec';
เปลี่ยนเป็น Web App URL จริงจาก Apps Script แล้วอัปโหลดไฟล์ขึ้น GitHub ครั้งเดียว — หลังจากนั้นทุกคนที่เปิดลิงก์จะเห็นข้อมูลเดียวกัน และแก้ไขได้ทันทีโดยไม่ต้องอัปโหลดไฟล์ใหม่ครับ
